<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    Naively testing the presence of a valid swap when clicking on the tolerance button
    Adding the "dropfruit" level type
    Correcting some bugs on fruit collection
    Changed implementation of nuts data in level data
    Changed Constants.FIELD_XLENGTH and YLENGTH in GameHandler (not needed in timing)
    Unstable check triggered when a level starts. Prior to this, falls didn't happen.
    -->
</resources>